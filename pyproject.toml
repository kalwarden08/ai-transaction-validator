[tool.poetry]
name = "ai-transaction-validator"
version = "1.0.0"
description = "Production-ready transaction validation web application"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = ""}]

[tool.poetry.dependencies]
python = "^3.8"
fastapi = ">=0.100.0"
uvicorn = ">=0.24.0"
pandas = ">=2.0.0"
python-multipart = ">=0.0.6"
aiofiles = ">=23.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
